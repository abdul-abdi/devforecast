# API Keys
OPENWEATHERMAP_API_KEY=
# Create a personal access token at https://github.com/settings/tokens
# Only public repository read-only access is needed (no access to your account data)
# For fine-grained tokens, only select "Public Repositories (read-only)" permission
GITHUB_API_TOKEN=
GEMINI_API_KEY=

# API Base URLs
NEXT_PUBLIC_OPENWEATHERMAP_BASE_URL=https://api.openweathermap.org/data/2.5
NEXT_PUBLIC_GITHUB_API_BASE_URL=https://api.github.com
NEXT_PUBLIC_GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta
